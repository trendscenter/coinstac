import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class ConsortiumPipeline extends Component { // eslint-disable-line
  render() {
    return (
      <div>
        Pipeline
      </div>
    );
  }
}
