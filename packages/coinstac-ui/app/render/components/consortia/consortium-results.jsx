import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class ConsortiumResults extends Component { // eslint-disable-line
  render() {
    return (
      <div>
        Results
      </div>
    );
  }
}
