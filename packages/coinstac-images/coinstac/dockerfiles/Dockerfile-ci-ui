FROM coinstacteam/coinstac:ci
RUN apt-get update && \
    apt-get install -y xvfb \
    libgbm1 \
    libxss1 \
    libnss3 \
    libgtk-3-dev \
    libasound2-dev \
    unzip \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /usr/local/src/coinstac/packages/coinstac-ui
